#include <iostream>
#include <bits/stdc++.h>
using namespace std;
void slove(){
    int n; cin>>n;
    int m; cin>>m;

    vector<int> a(n);
    vector<int> b(m);
 
    for (int i = 0; i < n; i++)cin>>a[i];
    for (int i = 0; i < m; i++)cin>>b[i];

    sort(b.begin() , b.end());

    int prev = -INFINITY;

    for (int  i = 0; i < count; i++)
    {
        /* code */
    }
    
    
}
int main()
{
    int t;
    cin >> t;

    while (t--)
    {
        slove();
    }

    return 0;
}